services:
  db:
    image: postgres:16-alpine
    container_name: anime-character-hub-db
    environment:
      POSTGRES_DB: anime_character_hub
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d anime_character_hub"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
